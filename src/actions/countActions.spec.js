// This is where action creators are tested

import React from 'react';
import { default as actions } from './countActions';
import { mount } from 'enzyme';

describe('(Action creator) actions', () => {
  let _component;

  it('Can do a cool action', () => {
  });
});
